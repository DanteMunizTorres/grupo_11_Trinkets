<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %> 
<body>

    <%- include('../partials/header') %> 
    
    <main>
        <div class="form-container">
            <h2 class="main-title-form">Ingresá tu mail y contraseña</h2>
            <form method="POST" action='/user/login' class="form">
                <div class="form-input">
                    <label for="email" class="input-label">Email</label>
                    <input type="email" id="email" name="email" placeholder="example@trinkets.com" value="<%= locals.old && old.email ? old.email : '' %>" class="input input-email">
                    <% if(errors.email) {%>
                        <p><%= errors.email.msg %> </p>
                    <% } %>    
                </div>


                <div class="form-input">
                    <label for="password" class="input-label">Contraseña</label>
                    <input type="password" id="password" name="password" placeholder="********" class="input input-password" minlength="8" maxlength="24">    
                    <% if(errors.password) {%>
                        <p><%= errors.password.msg %> </p>
                    <% } %>                   
                </div>

                <div class="form-input input-terms input-loguin">
                    
                    <input type="checkbox" id="remember" name="remember" value="true">
                    <label for="remember">Recordarme</label>    
              
                </div>

                <div class="form-input input-terms input-loguin">
                    
                    <button type="submit" class="main-button">Entrar</button>
                    
              
                </div>
                <a href="">¿Olvidaste tu contraseña?</a>
            </form>            
        </div>


    </main>
    <%- include('../partials/footer') %> 
</body>
</html>